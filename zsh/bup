### BREW + FZF
# Upgrade multiple packages at once, async

local upd=$(brew outdated | eval "fzf ${FZF_DEFAULT_OPTS} -m --header='[brew:update]'")

if [[ $upd ]]; then
  for prog in $(echo $upd)
  do brew upgrade $prog
  done
fi
